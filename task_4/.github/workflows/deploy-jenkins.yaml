name: Deploy Jenkins

on:
  push:
    branches: [ task_4 ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Echo (placeholder)
        run: echo "This would run deployment logic if cluster were accessible"
 